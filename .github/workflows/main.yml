# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  convert_via_pandoc:
    runs-on: ubuntu-18.04
    steps:
      - name: checkout repo
        uses: actions/checkout@v3
      - name: convert
        uses: docker://pandoc/core:2-ubuntu
        with:
          args: "-t gfm --extract-media=. ./ServoDrive-SetupGuide_EP.odt -o ServoDrive-SetupGuide_trial.md" # gets appended to pandoc command
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - name: upload result
        uses: actions/upload-artifact@v3

          
#  pandoc -t gfm --extract-media=. ServoDrive-SetupGuide_EP.odt -o ServoDrive-SetupGuide_trial.md
